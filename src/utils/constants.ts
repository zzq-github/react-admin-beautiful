/**
 * 通用常量定义
 */

/**
 * 全局通用状态枚举
 */
export enum CommonStatusEnum {
  ENABLE = 0,
  DISABLE = 1
}

/**
 * 菜单的类型枚举
 */
export enum MenuTypeEnum {
  DIR = 1,    // 目录
  MENU = 2,   // 菜单
  BUTTON = 3  // 按钮
}
